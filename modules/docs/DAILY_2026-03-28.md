# Daily Report — 28/03/2026

## O que fizemos

### Fix crítico no cálculo de chuva (ETL)
- **Arquivo:** `etl/application/analyzer/rain.py`
- PRECmax é um valor **acumulativo** no dado-fonte, não instantâneo. Corrigido o analyzer para calcular a diferença `T - T-2` (delta de 1h em passos de 30min) antes de classificar a intensidade da chuva.
- Antes, o sistema comparava o acumulado bruto com os thresholds, gerando falsos alertas.

### Fix de overflow no horário (ETL)
- **Arquivo:** `etl/application/load.py`
- Segundos acima de 86.400 (ex: `90000` = 1h do dia seguinte) causavam hora inválida. Adicionado `% 86400` para normalizar antes da conversão `HH:MM`.

### Fix no parse de `data_referencia` (ETL)
- **Arquivo:** `etl/infra/database/aviso_repository.py`
- Campo vinha com timestamp completo (`YYYY-MM-DD HH:MM:SS`) e `date.fromisoformat` quebrava. Adicionado `[:10]` para extrair apenas a data.

### Fix no formato de data em templates (Notifications)
- **Arquivo:** `notifications/application/templates/base.py`
- `format_data_pt_br` passou a suportar strings com timestamp (`YYYY-MM-DD HH:MM:SS`), evitando retorno sem formatação nas mensagens enviadas.

### Infraestrutura: centralização do init-db
- Movidos `schema.sql` / `seed.sql` para `modules/init-db/` (compartilhado entre ETL e Notifications).
- Adicionado `seed_e2e.sql` para testes de ponta a ponta.
- Corrigidos os paths nos `docker-compose.yml` de ambos os módulos.

### Dockerfile do Notifications refatorado
- Migrado de `pip install -e .` para `uv` (alinhado com o ETL).
- Adicionado `build-essential` e variáveis de ambiente padrão (`PYTHONDONTWRITEBYTECODE`, `PYTHONUNBUFFERED`).

### Instrumentação de debug no pipeline de notificações
- Adicionados logs estruturados em `filter.py`, `consumer.py`, `dispatcher.py` e `whatsapp_sender.py` para rastrear o fluxo completo: quantos alertas passam pelo filtro, qual canal é acionado, resposta da Z-API.

### Documentação e infraestrutura local
- Criados `docs/FLUXO_E2E.md`, `docs/REGRAS_DE_NEGOCIO.md`, `docs/TESTE_E2E.md`.
- Adicionados `.env.example`, `Makefile` e `docker-compose.yml` raiz para facilitar o setup local.

---

## Próximos passos

- [ ] **Validar pipeline E2E** — rodar com `seed_e2e.sql` e confirmar que alertas de chuva chegam no WhatsApp com os valores corretos após o fix do delta PRECmax.
- [ ] **Remover logs de debug** — `[DEBUG *]` em `filter`, `consumer`, `dispatcher` e `whatsapp_sender` são instrumentação temporária; limpar antes de ir para produção.
- [ ] **Remover `print` de credenciais** — `whatsapp_sender.py` tem dois `print` expondo `INSTANCE`, `TOKEN` e `CLIENT_TOKEN`. Remover antes de qualquer deploy.
- [ ] **Testar Dockerfile do Notifications** — rebuild após migração para `uv`, verificar se o ambiente sobe sem erros.
- [ ] **Revisar thresholds de chuva** — com o cálculo corrigido (delta em vez de acumulado), os limites de classificação podem precisar de ajuste fino.
